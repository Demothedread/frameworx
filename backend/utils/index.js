/**
 * Utility aggregation file for backend
 * Add shared logic (e.g. CMS blog fetch, file helpers, DB, etc) here.
 */
const logger = require('../../shared/utils/logger');

module.exports = {
  logger,
  // e.g. getUserFromSession, getCmsPosts
};
