import React from 'react';

/**
 * Minimal placeholder for Create React App Channel.
 * Should serve as a template for adding new channels easily.
 */
function CraChannel() {
  return (
    <div style={{ padding: 24 }}>
      <h2>CRA Channel</h2>
      <p>This is a minimal, modular channel placeholder inspired by Create React App.</p>
      <p>You can copy and paste this folder to bootstrap new channels.</p>
    </div>
  );
}

export default CraChannel;
